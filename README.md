Queue is a lightweight command line to-do app.

Example usage
```bash
queue add "Finish math homework"
queue add "Apply for summer program" -d "Need transcript first"
queue ls
queue rm 3
queue add "Apply for summer program" -i 3
queue clear
```

The program is written in Go.
